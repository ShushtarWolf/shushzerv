# Prisma / SQLite (local file, gitignored)
DATABASE_URL="file:./dev.db"

# nuxt-auth-utils sealed session secret (must be at least 32 characters)
NUXT_SESSION_PASSWORD="change-me-to-a-long-random-32char-string"
