# Railway — uses Dockerfile (Prisma + SQLite on /data volume)
# https://docs.railway.com/guides/deploy

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
